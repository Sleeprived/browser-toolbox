{
  "name": "browser-toolbox",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "description": "Twelve no-upload, fully client-side browser tools (QR, EXIF cleaner, passphrase, password vault, CSV<->JSON, palette, cron, encode/decode, JWT decoder, image resizer, Morse code, cipher studio) as an installable PWA.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "jsqr": "^1.4.0",
    "piexifjs": "^1.0.6",
    "qrcode-generator": "^2.0.4",
    "vitest": "^4.1.9"
  }
}
