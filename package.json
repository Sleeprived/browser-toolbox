{
  "name": "browser-toolbox",
  "version": "1.2.2",
  "private": true,
  "type": "module",
  "description": "Ten no-upload, fully client-side browser tools (QR, EXIF cleaner, passphrase, password vault, CSV<->JSON, palette, cron, encode/decode, JWT decoder, image resizer) as an installable PWA.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "piexifjs": "^1.0.6",
    "qrcode-generator": "^2.0.4",
    "vitest": "^4.1.9"
  }
}
