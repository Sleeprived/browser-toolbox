{
  "name": "browser-toolbox",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "Eleven no-upload, fully client-side browser tools (QR, EXIF cleaner, passphrase, password vault, CSV<->JSON, palette, cron, encode/decode, JWT decoder, image resizer, Morse code) as an installable PWA.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "jsqr": "^1.4.0",
    "piexifjs": "^1.0.6",
    "qrcode-generator": "^2.0.4",
    "vitest": "^4.1.9"
  }
}
